defaults:
- _autrainer_
- _self_
device: cuda
results_dir: results
experiment_id: baseline_clean
iterations: 15
hydra:
  sweeper:
    params:
      +seed: 0
      +batch_size: 64
      +learning_rate: 0.001
      dataset: TIMIT-sentencetype-16k
      model: Cnn10-32k-T
      optimizer: Adam
